[project]
name = "ida-codemode-mcp"
version = "0.1.0"
description = "Code Mode MCP server for the ida-domain API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ida-domain>=0.4.0",
    "zeromcp>=1.4.0",
]

[tool.uv.sources]
ida-domain = { git = "https://github.com/HexRaysSA/ida-domain", branch = "include-docs-examples" }
