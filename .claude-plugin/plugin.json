{
  "name": "ida-mcp",
  "version": "0.4.1",
  "description": "Official IDA MCP server.",
  "mcpServers": {
    "ida": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "ida-codemode-mcp",
        "--agent",
        "claude",
        "--install-plugin"
      ],
      "env": {
        "IDA_CODEMODE_ID": "${IDA_CODEMODE_ID:-}",
        "IDAUSR": "${IDAUSR:-}",
        "IDA_CODEMODE_STATE_DIR": "${IDA_CODEMODE_STATE_DIR:-}"
      }
    }
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__(.*[_:])?ida__.*",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --project ${CLAUDE_PLUGIN_ROOT} ida-codemode-mcp --report-session=claude"
          }
        ]
      }
    ]
  }
}
